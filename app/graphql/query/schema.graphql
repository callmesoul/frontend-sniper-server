
type Query {
  user(id: ID!): User
  app(id:ID!):App,
  userApps:[App],
  appErrors(id:ID!,page:Int,limit:Int):AppError,
  userErrors:[Error]
  emailConfig:EmailConfig,
  emailList(page:Int,limit:Int,appId:Int):EmailList
}